spring:
  application:
    name: onlyone-portal-edgeserver
  cloud:
    config:
      discovery:
        enabled: true
      fail-fast: true
management:
  metrics:
    export:
      prometheus:
        pushgateway:
          base-url: localhost:9095
